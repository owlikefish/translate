为了此文档的用途，使用如下的术语和定义

# 类型
此规范条款6中定义的数据值的集合

# 原始值
条款6中定义的类型undefined，null，boolean，number，symbol或string其中之一的成员
注：原始值是在语言实现的最底层直接表示的数据

# 对象
object类型的成员
注：对象是属性的集合，并且拥有单个的原型对象。原型可能是空值。

# 构造器
创建和初始化对象的函数对象
注：构造函数的原型属性是一个用来实现继承和共享属性的原型对象。

# 原型
为其他对象提供共享属性的对象。
注：当一个构造器创建了一个对象，该对象会暗含地引用构造器的用来解决属性引用的原型属性。构造器的原型属性可用程序表达式constructor.prototype来引用，添加到对象原型的属性可通过继承被共享，因为所有的对象共享该原型。另外，使用object.create内建函数可以创建一个明确指定原型的新对象。

# 普通对象
为了必须被所有对象支持的必要内部方法而拥有默认行为的对象

# 异常对象
没有用于必须被所有对象支持的一个或多个必要内部方法的默认行为的对象
注：任何不是普通对象的对象则是一个异常对象

# 标准对象
由此规范定义了语义的对象

# 内建对象
由ECMAScript实现指定和提供的对象
注：在此规范中定义了标准内建对象。一个ECMAScript实现可能会指定和提供额外的内建对象种类。内建的构造器既是一个内建的对象，也是一个构造器。

# undefined值
当一个变量没有被赋值时的原始值

# undefined类型
只有唯一值是undefined值的类型

# null值
表示任意对象值有意空缺的原始值

# null类型
拥有唯一值是null值的类型

# boolean值
boolean类型的成员
注：只有两个boolean值，true和false

# boolean类型
由原始值true和false组成的类型

# boolean对象
作为标准内建boolean构造器的一个实例的对象类型的成员
注：boolean对象在new表达式中使用boolean构造器创建，提供一个boolean值作为参数。返回的对象只有一个值是boolean值的内部小槽（slot)

# string值
作为一个有限的0个或多个16位无符号整数的有序序列的原始值。
注：string值是string类型的成员。序列中的每一个整数通常表示一个单独的UTF-16文本的的16-bit单元。然而，除了值必须是16-bit无符号整数外，ECMAScript没有在值上放任何限制和需求。

# string类型
所有可能的string值的集合

# string对象
作为标准内建string构造器的实例的对象类型成员。
注：string对象在new表达式中通过使用string构造函数来创建，需要提供一个string值作为参数。返回的对象有一个值是string值的内部slot。一个string对象可通过调用String构造函数来胁迫成string值。

# number值
符合双精度64-bit二进制格式IEEE 754-2008值的原始值
注：number值是number类型的一个成员，并且直接表示一个数值

# number类型
所有可能的数字值的集合，这些数字值包括特殊的NaN，正infinity，和负infinity

# number对象
作为标准内建number构造函数实例的object类型的成员
注：number对象在new表达式中通过使用number构造函数创建，提供一个number值作为参数。返回的对象有一个值是number值的内部slot。一个number对象可通过调用number构造函数来胁迫成number值。

# infinity
表示正无穷的number值

# NaN
IEEE 754-2008 “Not-a-Number”值的number值

# symbol值
表示一个独有的，非字符串对象属性键的原始值

# symbol类型
所有可能的symbol值的集合

# symbol对象
作为标准内建symbol构造函数实例的对象类型成员

# 函数
可被作为子程序调用的对象类型成员
注：除了它的属性之外，函数还包含了可执行的代码，和决定被调用时如何行动的状态。一个函数的代码可能是也可能不是由ECMAScript写的。

# 内建函数
内建的函数对象
注：内建函数的例子有parseInt和Math.exp。一个实现可能会提供本规范中没有描述的依赖实现的内建函数。

# 属性
对象中关联一个键（或者是字符串值，或者是符号值）和一个值的部分。
注：根据属性的形式，属性值可能直接表示一个数据值（原始值，对象，函数对象），也可能间接由一对访问函数表示

# 方法
值是函数的属性
注：当一个函数被称作一个对象的方法，该对象则被传递了该函数作为它的this的值

# 内建方法
是内建函数的方法
注：标准的内建方法在此规范中有定义，而一个ECMAScript实现可能指定和提供了其他额外的内建方法

# attribute性质
定义了属性一些特质的内部值

# 自有属性
直接被它的对象包含的属性

# 继承属性
不是一个对象的自有属性，而是该对象原型的属性，（或者被原型拥有，或者被原型继承而来）
